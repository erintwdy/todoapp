{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run todoapp (Spring Boot)",
            "type": "shell",
            "command": "./mvnw spring-boot:run",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
    }